name: Deploy Inventory Frontend

on:
  workflow_dispatch:
  push:
    branches:
      - develop
      - qa

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '22'

      - uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.CONTABO_SSH_KEY }}

      - name: Install + build
        run: |
          npm install --legacy-peer-deps --no-audit --no-fund
          mkdir -p node_modules/@so360
          rm -rf node_modules/@so360/* && cp -r .ci-stubs/@so360/* node_modules/@so360/
          rm -rf ../../so360-shell-fe 2>/dev/null; true
          for pkg in shell-context design-system event-bus formatters; do
            mkdir -p ../../so360-shell-fe/packages/$pkg/dist
            cp .ci-stubs/@so360/$pkg/index.js ../../so360-shell-fe/packages/$pkg/dist/index.js
          done
          if [ "$GITHUB_REF" = "refs/heads/develop" ]; then
            printf '%s\n' \
                "VITE_BASE_URL=https://dev-cdn.skyoffice360.com/inventory/" \
                "VITE_SO360_CORE_API=https://dev.api.skyoffice360.com/core" \
                "VITE_SO360_INVENTORY_API=https://dev.api.skyoffice360.com/inventory" \
                "VITE_ENVIRONMENT=cloud" \
                > .env
          elif [ "$GITHUB_REF" = "refs/heads/qa" ]; then
            printf '%s\n' \
                "VITE_BASE_URL=https://staging.inventory.skyoffice360.com/" \
                "VITE_SO360_CORE_API=https://staging.api.skyoffice360.com/core" \
                "VITE_SO360_INVENTORY_API=https://staging.api.skyoffice360.com/inventory" \
                "VITE_ENVIRONMENT=cloud" \
                > .env
          fi
          npm run build

      - name: Deploy to Dev
        if: github.ref == 'refs/heads/develop'
        env:
          SERVER_IP: ${{ secrets.SERVER_IP }}
          NGINX_ROOT: /var/www/dev-cdn.skyoffice360.com/html/inventory
        run: |
          ssh-keyscan -H $SERVER_IP >> ~/.ssh/known_hosts
          ssh root@$SERVER_IP "mkdir -p '$NGINX_ROOT'"
          rsync -avz --delete dist/ "root@$SERVER_IP:$NGINX_ROOT/"
          ssh root@$SERVER_IP "nginx -s reload"
          echo "Deployed to dev-cdn.skyoffice360.com/inventory"

      - name: Deploy to Staging
        if: github.ref == 'refs/heads/qa'
        env:
          SERVER_IP: ${{ secrets.SERVER_IP }}
          NGINX_ROOT: /var/www/staging.inventory.skyoffice360.com/html
        run: |
          ssh-keyscan -H $SERVER_IP >> ~/.ssh/known_hosts
          ssh root@$SERVER_IP "mkdir -p '$NGINX_ROOT'"
          rsync -avz --delete dist/ "root@$SERVER_IP:$NGINX_ROOT/"
          ssh root@$SERVER_IP "nginx -s reload"
          echo "Deployed to staging.inventory.skyoffice360.com"
